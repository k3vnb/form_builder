export type InputState = {
  invalid?: boolean;
  disabled?: boolean;
  readOnly?: boolean;
  active?: boolean;
  selected?: boolean;
}

export type InputStateStyleProps = {
  baseStyles?: string;
  default?: string;
  invalid?: string;
  disabled?: string;
  readOnly?: string;
  active?: string;
  selected?: string;
}
